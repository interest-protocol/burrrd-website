export interface ThemeProps {
  theme: {
    colors: {
      primary: string;
      secondary: string;
      tertiary: string;
      quaternary: string;
      quinary: string;
      senary: string;
      septenary: string;
    };
    gradient: {
      radialGradientViolet: string;
    };
  };
}
